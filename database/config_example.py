def config(db='healthcare'):
    if db == 'healthcare':
        return {
            "host": "localhost",
            "database": "test",
            'user': "user",
            "password": "password"
        }
    else:
        return {
            "host": "localhost",
            "database": "test",
            'user': "user",
            "password": "password"
        }
